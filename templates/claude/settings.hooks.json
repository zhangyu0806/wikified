{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "^(startup|resume|clear|compact)$",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/env LLM_WIKI_AGENT_PROFILE=claude LLM_WIKI_DOMAIN=work \"$HOME/.local/bin/llm-wiki-session-start\" --format claude --max-chars 2500",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
