{
  "description": "Inject bounded, redacted Wikified critical facts when Codex starts, resumes, clears, or compacts a session.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "^(startup|resume|clear|compact)$",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/env LLM_WIKI_AGENT_PROFILE=codex LLM_WIKI_DOMAIN=work \"$HOME/.local/bin/llm-wiki-session-start\" --format codex --max-chars 2500",
            "commandWindows": "wsl.exe -d Ubuntu -e /usr/bin/env LLM_WIKI_AGENT_PROFILE=codex LLM_WIKI_DOMAIN=work /bin/sh -lc 'exec \"$HOME/.local/bin/llm-wiki-session-start\" --format codex --max-chars 2500'",
            "timeout": 10,
            "statusMessage": "Loading stable Wikified context",
            "additionalContextLimit": 2500
          }
        ]
      }
    ]
  }
}
